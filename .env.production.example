# =============================================================================
# LixyLearning - Variables de entorno para producción (AWS/EC2)
# =============================================================================
# Copia este archivo como .env en el servidor y reemplaza los valores.
# NUNCA subas el archivo .env con valores reales a Git.
# =============================================================================

# --- Frontend (Next.js) - URLs públicas ---
# URL completa del backend (con protocolo https:// en producción)
NEXT_PUBLIC_BACKEND_URL=https://tu-dominio.duckdns.org
# URL del frontend (misma que backend si usas reverse proxy)
NEXT_PUBLIC_FRONTEND_URL=https://tu-dominio.duckdns.org

# Opcional (reservada; la UI de contacto no la muestra).
NEXT_PUBLIC_CONTACT_EMAIL=contacto@tu-dominio.org

# --- Formulario de contacto (API Resend; sin SMTP) ---
# https://resend.com/docs/send-with-python
# RESEND_API_KEY=re_xxxxxxxx
# RESEND_FROM_EMAIL=Tu App <notificaciones@tu-dominio-verificado.com>
# CONTACT_TO_EMAIL=donde_recibes@ejemplo.com

# --- Base de datos PostgreSQL ---
# En Docker Compose, el host es el nombre del servicio: postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=tu_password_seguro
POSTGRES_DB=lixylearning_db
DATABASE_URL=postgresql://postgres:tu_password_seguro@postgres:5432/lixylearning_db

# --- Autenticación ---
# Genera uno con: openssl rand -base64 32
JWT_SECRET=tu_jwt_secret_muy_largo_y_aleatorio

# --- AWS Cognito (opcional) ---
# Si se configuran, el frontend usará Cognito para login/signup.
# Dejar vacíos para usar autenticación JWT tradicional.
# COGNITO_REGION=us-east-1
# COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
# COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# --- APIs de Inteligencia Artificial ---
# Google Gemini (generación de contenido)
GEMINI_API_KEY=tu_gemini_api_key

# OpenAI (oraciones, traducciones, historias)
OPENAI_API_KEY=tu_openai_api_key

# --- Google Cloud / APIs de Google ---
# Text-to-Speech (síntesis de voz)
GOOGLE_API_KEY=tu_google_api_key

# --- Pixabay (imágenes para flashcards) ---
# API gratuita, sin facturación. Obtener en: https://pixabay.com/api/docs/
PIXABAY_API_KEY=tu_pixabay_api_key
